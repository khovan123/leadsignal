name: Legacy Production Readiness

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validation now runs in .github/workflows/ci.yml"
