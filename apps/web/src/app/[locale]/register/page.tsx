import Link from 'next/link';
import { registerAction } from '../auth-actions';

export default async function RegisterPage({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{error?:string}>}){const {locale}=await params;const {error}=await searchParams;return <div className="mx-auto max-w-md space-y-6"><div><h1 className="text-3xl font-semibold">Create your LeadSignal account</h1><p className="mt-2 text-slate-400">A private workspace will be created automatically.</p></div>{error&&<div className="rounded-lg border border-red-800 bg-red-950/40 p-3 text-sm text-red-300">{decodeURIComponent(error)}</div>}<form action={registerAction} className="panel space-y-4 p-6"><input type="hidden" name="locale" value={locale}/><label className="block text-sm">Display name<input name="displayName" required autoComplete="name" className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2"/></label><label className="block text-sm">Email<input name="email" type="email" required autoComplete="email" className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2"/></label><label className="block text-sm">Password<input name="password" type="password" required minLength={12} autoComplete="new-password" className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-900 px-3 py-2"/><span className="mt-1 block text-xs text-slate-500">At least 12 characters.</span></label><button className="w-full rounded-lg bg-violet-600 px-4 py-2 font-medium">Create account</button></form><p className="text-sm text-slate-400">Already registered? <Link className="text-violet-300" href={`/${locale}/login`}>Sign in</Link></p></div>}
