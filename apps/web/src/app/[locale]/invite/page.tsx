import { acceptInvitationAction } from '../auth-actions';

export default async function InvitePage({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{token?:string}>}){const {locale}=await params;const {token}=await searchParams;return <div className="mx-auto max-w-lg space-y-6"><h1 className="text-3xl font-semibold">Join the workspace</h1><p className="text-slate-400">Accepting this invitation adds your authenticated account to the shared LeadSignal workspace.</p>{token?<form action={acceptInvitationAction} className="panel p-6"><input type="hidden" name="locale" value={locale}/><input type="hidden" name="token" value={token}/><button className="w-full rounded-lg bg-violet-600 px-4 py-2 font-medium">Accept invitation</button></form>:<div className="rounded-lg border border-red-800 bg-red-950/40 p-4 text-red-300">The invitation token is missing.</div>}</div>}
