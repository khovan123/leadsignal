{
  "name": "@leadsignal/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx --tsconfig tsconfig.json watch src/dev.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo worker:ok"
  },
  "dependencies": {
    "@leadsignal/api": "workspace:*",
    "@nestjs/core": "^11.0.0",
    "@prisma/client": "^7.0.0",
    "bullmq": "^5.0.0",
    "reflect-metadata": "^0.2.2",
    "ioredis": "^5.4.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  },
  "type": "module"
}
